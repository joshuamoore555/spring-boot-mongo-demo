server.port=${SERVER_PORT:8080}

#spring.data.mongodb.uri: ${DB_PROTO:mongodb}://${DB_USERNAME}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT:27017}/${DB_NAME:employees}
spring.data.mongodb.host=${DB_HOST}
spring.data.mongodb.port=${DB_PORT:27017}
spring.data.mongodb.database=${DB_NAME:employees}
#spring.data.mongodb.username=${DB_USERNAME}
#spring.data.mongodb.password=${DB_PASSWORD}
